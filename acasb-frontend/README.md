# ACASB 前端示例

## 概述

这是一个使用 Vue.js 3 和 Axios 构建的 ACASB 前端示例，展示了如何调用 ACASB API 进行批量上传和数据查询。

## 功能特性

- ✅ 拖拽上传图片
- ✅ 批量上传处理
- ✅ 数据排序查询
- ✅ 建筑类型筛选（皇室/平民）
- ✅ 实时统计显示
- ✅ 响应式设计
- ✅ 美观的渐变 UI

## 快速开始

### 方法 1：直接打开

1. 在浏览器中直接打开 `index.html` 文件
2. 即可使用所有功能

### 方法 2：本地服务器

1. 打开终端，进入 `acasb-frontend` 目录
2. 运行本地服务器：

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js (需要先安装 http-server)
npx http-server -p 8000
```

3. 在浏览器中访问 `http://localhost:8000`

## 使用说明

### 1. 批量上传图片

#### 方式一：点击上传
1. 点击上传区域
2. 选择一张或多张图片
3. 点击"开始上传"按钮

#### 方式二：拖拽上传
1. 将图片拖拽到上传区域
2. 释放鼠标
3. 点击"开始上传"按钮

### 2. 数据查询与排序

1. **选择排序字段**：从下拉菜单中选择要排序的特征字段
   - 皇家比例
   - 熵值
   - 边缘密度
   - 对比度
   - 不相似度
   - 同质性
   - 角二阶矩
   - 色相均值/标准差
   - 饱和度均值/标准差
   - 明度均值/标准差
   - 各颜色比例

2. **选择排序方式**：
   - 降序：从大到小
   - 升序：从小到大

3. **设置返回数量**：输入要返回的记录数（1-100）

4. **筛选建筑类型**（可选）：
   - 全部：查询所有记录
   - 皇室建筑：只查询皇室建筑
   - 平民建筑：只查询平民建筑

5. **点击"查询"按钮**：获取排序后的数据

### 3. 查看结果

- **统计卡片**：顶部显示总记录数、皇室建筑数、平民建筑数、预测准确率
- **结果表格**：显示详细的查询结果
  - ID：记录 ID
  - 图片路径：图片的完整路径
  - 预测结果：皇室建筑 🏰 或 平民建筑 🏠
  - 置信度：预测的置信度（百分比）
  - 皇家比例：皇家色彩占比
  - 熵值：图像纹理复杂度
  - 边缘密度：图像结构复杂度

## 技术栈

- **Vue.js 3**：前端框架
- **Axios**：HTTP 客户端
- **CSS3**：样式设计
- **HTML5**：拖拽上传功能

## API 接口

### 批量上传接口
- **端点**：`POST http://localhost:8080/data/batch`
- **请求格式**：`multipart/form-data`
- **参数**：`files`（文件数组）

### 数据查询接口
- **端点**：`GET http://localhost:8080/data/list`
- **请求格式**：查询参数
- **参数**：
  - `field`：排序字段
  - `order`：排序方式（asc/desc）
  - `limit`：返回记录数
  - `prediction`：建筑类型筛选（royal/civilian）

## 代码示例

### 批量上传

```javascript
const uploadFiles = async (files) => {
    const formData = new FormData();
    files.forEach(file => {
        formData.append('files', file);
    });
    
    try {
        const response = await axios.post('http://localhost:8080/data/batch', formData, {
            headers: {
                'Content-Type': 'multipart/form-data'
            }
        });
        
        const data = response.data;
        console.log(`上传完成！成功 ${data.successCount} 张，失败 ${data.failureCount} 张`);
        return data;
    } catch (error) {
        console.error('上传失败:', error);
        throw error;
    }
};
```

### 数据查询

```javascript
const fetchData = async () => {
    try {
        const params = {
            field: 'royalRatio',
            order: 'desc',
            limit: 10,
            prediction: 'royal'
        };
        
        const response = await axios.get('http://localhost:8080/data/list', { params });
        return response.data;
    } catch (error) {
        console.error('查询失败:', error);
        throw error;
    }
};
```

## 自定义开发

### 修改 API 地址

在 `index.html` 中搜索 `http://localhost:8080`，替换为你的 API 地址：

```javascript
// 批量上传
const response = await axios.post('http://your-api-url:8080/data/batch', formData, {

// 数据查询
const response = await axios.get('http://your-api-url:8080/data/list', { params });
```

### 修改样式

在 `<style>` 标签中修改 CSS 样式：

```css
/* 修改主题颜色 */
body {
    background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}

.btn {
    background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}
```

### 添加新功能

1. 在 `setup()` 函数中添加新的响应式变量
2. 在模板中添加对应的 UI 元素
3. 添加相应的事件处理函数

## 常见问题

### Q: 上传失败怎么办？
A: 检查以下几点：
1. 确保 Spring Boot 服务已启动（端口 8080）
2. 确保 Python 服务已启动（端口 5000）
3. 检查浏览器控制台是否有错误信息
4. 检查网络连接

### Q: 查询不到数据？
A: 检查以下几点：
1. 确保数据库中有数据
2. 检查查询参数是否正确
3. 查看浏览器控制台是否有错误信息

### Q: 如何修改默认排序字段？
A: 在 `filters` 对象中修改默认值：

```javascript
const filters = ref({
    field: 'royalRatio',  // 修改这里
    order: 'desc',
    limit: 10,
    prediction: ''
});
```

## 许可证

MIT License

## 联系方式

如有问题或建议，请联系开发团队。
