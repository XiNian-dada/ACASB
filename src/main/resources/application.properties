spring.application.name=ACASB
server.port=8080

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Temporary Folder Configuration
app.temp-folder=E:/Code/ACASB/temp

# Database Configuration (Optional - currently disabled in main application)
# spring.datasource.url=jdbc:mysql://localhost:3306/acasb
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis-Plus Configuration
# mybatis-plus.configuration.map-underscore-to-camel-case=true
# mybatis-plus.global-config.db-config.logic-delete-field=deleted
# mybatis-plus.global-config.db-config.logic-delete-value=1
# mybatis-plus.global-config.db-config.logic-not-delete-value=0
